<%= form_with model: [ journal, DreamLog.new(dream: true) ], data: { controller: "dream-log-form"} do |form| %>
  <div class="card-static flex flex-col space-y-6">
    <%= render 'dream_logs/forms/have_dream_toggle', form: form %>
    <div data-dream-log-form-target="remember" class="hidden">
      <%= render 'dream_logs/forms/remember_dream_toggle', form: form %>
      <%= render 'dream_logs/forms/dream_body_form', form: form %>
    </div>
    <p class="flex justify-between">
      <%= form.label :sleep_time, class: "input-label" %>
      <%= form.number_field :sleep_time, step: 0.5, min: 0, class: "text-field-small" %>
    </p>
    <div class="flex">
      <%= form.submit class: "flex-grow btn-blue" %>
    </div>
  </div>
<% end %>