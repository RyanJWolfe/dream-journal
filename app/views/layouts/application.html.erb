<!DOCTYPE html>
<html>
<head>
  <title>Dream Journal</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body data-controller="menu" class="body">
<div class="flex flex-col h-screen" data-action="click->menu#hide">
  <header class="sticky top-0">
    <%= render 'layouts/navbar/navbar' %>
  </header>
  <main class="container mt-10 px-5 mx-auto mb-auto">
    <div class="flex justify-center items-center">
      <% if notice.present? %>
        <p class="notice" id="notice"><%= notice %></p>
      <% end %>
    </div>
    <div class="flex justify-center items-center">
      <% if alert.present? %>
        <p class="alert" id="alert"><%= alert %></p>
      <% end %>
    </div>
    <%= yield %>
  </main>
  <footer class="mt-12">
    <%= render 'layouts/footer' %>
  </footer>
</div>
</body>
</html>
