<nav class="bg-white border-gray-200 px-2 sm:px-4 py-2.5 dark:bg-gray-800">
  <div class="container flex justify-between items-center mx-auto">
    <a href="/" class="flex">
      <%= image_tag('dream_journal_icon.png', alt: "Logo", class: "mr-3 h-10") %>
      <span class="hidden md:block navbar-title">Dream Journal</span>
    </a>
    <%= render 'layouts/user_menu' if user_signed_in? %>
    <%= render 'layouts/navbar_login' unless user_signed_in? %>
    <%= render 'layouts/navbar_menu' %>
  </div>
</nav>